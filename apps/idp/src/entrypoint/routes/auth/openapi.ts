export const authOpenApiTagName = 'auth'

export const authOpenApiTag = {
  name: authOpenApiTagName,
  description:
    'IDP-owned authentication wrappers that delegate credential, cookie, and session internals to Better Auth.',
} as const
