export const classificacaoOpenApiTagName = 'classificacao'

export const classificacaoOpenApiTag = {
  name: classificacaoOpenApiTagName,
  description:
    'Classificações de risco de uma unidade: persiste o resultado, as respostas e o contexto (fatores) para rastreabilidade.',
} as const
