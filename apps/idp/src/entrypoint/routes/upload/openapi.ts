export const uploadOpenApiTagName = 'upload'

export const uploadOpenApiTag = {
  name: uploadOpenApiTagName,
  description:
    'Upload de documentos (PDF ≤ 5 MB) para o storage de objetos (Tigris). Retorna uma chave usada para anexar o documento a um processo (N1-01) e para gerar URLs de visualização temporárias.',
} as const
